#!/bin/bash

hit_test() {
  case "$1" in
    p | prep)
      bash "$HIT_SCRIPT_DIR/test/prep"
      ;;
    help)
      echo "Usage: hit test [prep]"
      ;;
    *)
      bash "$HIT_SCRIPT_DIR/test/env"
      ;;
  esac
}
