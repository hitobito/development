#!/bin/bash

hit_db() {
    case "$1" in
        console)
            echo "Connecting to database console..."
            bash "$HIT_SCRIPT_DIR/db/console"
            ;;
        *)
            echo "Usage: hit db {console}"
            return 1
            ;;
    esac
}
