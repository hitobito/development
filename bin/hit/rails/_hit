#!/bin/bash

hit_rails() {
  case "$1" in
    attach)
      bash "$HIT_SCRIPT_DIR/rails/attach"
      ;;
    console)
      bash "$HIT_SCRIPT_DIR/rails/console"
      ;;
    bash)
      bash "$HIT_SCRIPT_DIR/rails/bash"
      ;;
    routes)
      bash "$HIT_SCRIPT_DIR/rails/routes"
      ;;
    *)
      echo "Usage: hit rails {attach, console, bash, routes}"
      return 1
      ;;
  esac
}
